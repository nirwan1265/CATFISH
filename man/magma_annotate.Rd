% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magma_wrappers.R
\name{magma_annotate}
\alias{magma_annotate}
\title{Run MAGMA SNP-to-gene annotation}
\usage{
magma_annotate(
  stats_file,
  rename_columns,
  gene_loc = NULL,
  out_prefix,
  out_dir = NULL,
  window = NULL,
  species = NULL,
  sep = NULL,
  nonhuman = NULL,
  filter_snps = NULL,
  chr_map_path = NULL,
  strict_chr = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{stats_file}{Path to the GWAS summary statistics file.}

\item{rename_columns}{Named character vector mapping required MAGMA columns to
the column names in \code{stats_file}; must contain at least
\code{CHR}, \code{SNP}, and \code{POS} (e.g.
\code{c(CHR = "chr", SNP = "rs", POS = "ps", PVALUE = "p")}).}

\item{gene_loc}{Path to a MAGMA gene-location file (e.g. from
\code{\link{gff3_to_geneloc}}).}

\item{out_prefix}{Output file prefix.}

\item{out_dir}{Optional output directory. Created if it does not exist.}

\item{window}{Optional numeric vector \code{c(up, down)} giving the
upstream/downstream window in kb around each gene.}

\item{species}{Optional species tag (used to pick a built-in gene-location
file when \code{gene_loc} is not supplied).}

\item{sep}{Field separator for \code{stats_file}. If \code{NULL}, inferred
from the file extension.}

\item{nonhuman}{Logical or \code{NULL}; set \code{TRUE} for non-human data so
MAGMA does not assume human chromosome coding.}

\item{filter_snps}{Optional vector or file of SNPs to restrict the analysis to.}

\item{chr_map_path}{Optional path to a two-column chromosome-renaming map.}

\item{strict_chr}{Logical; if \code{TRUE}, error when the chromosome map is
missing an entry rather than passing the value through unchanged.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A list describing the annotation outputs, including the path to the
generated \code{.genes.annot} file (passed to \code{\link{magma_gene}}).
}
\description{
Wrapper around MAGMA's \code{--annotate} step. Maps SNPs from a GWAS summary
statistics file to genes using a MAGMA gene-location file, producing the
\code{.genes.annot} file consumed by \code{\link{magma_gene}}.
}
\seealso{
\code{\link{magma_gene}}, \code{\link{gff3_to_geneloc}}
}
